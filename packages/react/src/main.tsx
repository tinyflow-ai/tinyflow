import React from 'react'
import ReactDOM from 'react-dom/client'
import Tinyflow from './Tinyflow.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <Tinyflow/>
    </React.StrictMode>,
)
